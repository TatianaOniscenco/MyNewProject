<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Appender that writes to console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Template file appender, actual file path will be handled dynamically in Hooks -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <!-- This will be overridden by LogUtil to dynamically write log file per scenario -->
        <file>logs/default.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="info">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>