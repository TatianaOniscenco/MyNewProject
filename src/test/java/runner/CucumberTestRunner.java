package runner;

import io.cucumber.junit.platform.engine.Cucumber;

@Cucumber
public class CucumberTestRunner {
    // This class is used to run Cucumber tests with JUnit 5.
    // It automatically scans for feature files and step definitions in the classpath.
    // No additional code is needed here as the Cucumber engine will handle everything.
}
